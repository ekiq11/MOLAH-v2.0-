<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ===============================
         Permissions
         =============================== -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- Required for local_auth fingerprint login -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>

    <!-- ===============================
         Application
         =============================== -->
    <application
        android:label="MOLAH v2"
        android:name="android.app.Application"
        android:icon="@mipmap/ic_launcher">

        <!-- ===========================
             Main Flutter Activity
             ===========================

             FIX NOTES:
             - android:theme="@style/LaunchTheme" → styles.xml sudah diperbaiki
               agar tidak conflict dengan setStatusBarColor (deprecated)
             - Tidak ada android:statusBarColor / android:navigationBarColor di theme
               karena Flutter + WindowCompat yang handle via MainActivity.kt
             - configChanges sudah lengkap termasuk 'navigation' untuk handle
               gesture navigation di Samsung One UI / MIUI
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="
                orientation|
                keyboardHidden|
                keyboard|
                screenSize|
                smallestScreenSize|
                locale|
                layoutDirection|
                fontScale|
                screenLayout|
                density|
                uiMode|
                navigation">

            <!--
                NormalTheme: Dipakai setelah Flutter engine siap.
                Ini override LaunchTheme tanpa menyentuh statusBarColor.
            -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>

            <!--
                FIX: Tambahkan SplashScreenDrawable untuk mencegah white flash
                saat transisi launch → Flutter di Samsung/OPPO/Xiaomi.
                Pastikan @drawable/launch_background ada di res/drawable/.
            -->

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- ===========================
             WorkManager Background Service
             =========================== -->
        <service
            android:name="be.tramckrijte.workmanager.BackgroundService"
            android:exported="false"/>

        <!-- ===========================
             Flutter Embedding v2
             =========================== -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>

        <!-- ===========================
             AndroidX Startup (WorkManager)
             =========================== -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"/>
        </provider>

    </application>

    <!-- ===============================
         Package visibility (Android 11+)
         =============================== -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>

</manifest>